 <nav>
      <ul>
        <li><%= link_to 'Home', root_path %></li>
        <li><%= link_to 'About', static_page_path(title: 'About') %></li>
        <li><%= link_to 'Contact', static_page_path(title: 'Contact') %></li>
        <li><%= link_to 'All Products', products_path %></li>
        <li><%= link_to 'Cart', cart_path, class: 'btn btn-primary' %></li>
        <% unless user_signed_in? %>
          <li><%= link_to "Sign in", new_user_session_path, class: "btn btn-primary" %></li>
        <% end %>
        <% if user_signed_in? %>
          <li><%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete } %></li>
          <li>
            <% if current_user.avatar.attached? %>
              <%= image_tag current_user.avatar, alt: "Avatar", class: "avatar-image" %>
            <% end %>
          </li>
        <% end %>
      </ul>
    </nav>